# Detect Cycle in Linked List

## Problem
Determine if a linked list contains a cycle.

## Input
- Linked list head

## Output
- "yes" if there is a cycle, otherwise "no"

## Constraints
- Up to 10^5 nodes

## Hint
- Use Floyd's cycle detection (slow/fast pointers).
